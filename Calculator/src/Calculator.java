import java.lang.reflect.Array;

public class Calculator {
	int[] numbers = {2,4,6,3};
	public int[] multiply(int[] someArray) {
		String output = "";
		if (a * b == c) {
			output += a + "*" + b + " = " + c;
		}
		if (a * c == b) {
			output += a + "*" + c + " = " + b;
		}
		
	}

}
